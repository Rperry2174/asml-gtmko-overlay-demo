{
  "name": "yield-impact-analyst",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Prices a caught overlay miss for the ASML GTMKO demo and appends the impact-log row. Runs on the experimental grokbot runtime.",
  "scripts": {
    "dev": "agent-sdk dev",
    "serve": "agent-sdk serve --mode single",
    "validate": "agent-sdk validate",
    "info": "agent-sdk info",
    "check": "tsc --noEmit",
    "test": "node scripts/check-grokbot-invariants.mjs"
  },
  "dependencies": {
    "@cursor/july": "^0.1.102"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "typescript": "^5.8.0"
  }
}
