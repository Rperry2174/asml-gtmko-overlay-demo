{
  "name": "lot-incident-owner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Red-lot intake for the ASML GTMKO demo — acks the catch and hands the price to yield-impact-analyst. Runs on the experimental grokbot runtime.",
  "scripts": {
    "dev": "agent-sdk dev",
    "serve": "agent-sdk serve --mode single",
    "validate": "agent-sdk validate",
    "info": "agent-sdk info",
    "check": "tsc --noEmit",
    "test": "node scripts/check-grokbot-invariants.mjs"
  },
  "dependencies": {
    "@cursor/july": "^0.1.102"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "typescript": "^5.8.0"
  }
}
